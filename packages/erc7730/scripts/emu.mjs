// Phase 0 placeholder: starts Speculos with the Ethereum app and a test seed.
// Wired once `pip install speculos` and the app ELF are in place (07, fase 0).
console.error("ledger:emu is not wired yet — see packages/erc7730/README.md and 07 fase 0.");
process.exit(1);
