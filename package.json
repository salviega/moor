{
	"name": "moor",
	"version": "0.4.1",
	"private": true,
	"description": "Turns the waiting time of holding into productive capital. Ledger signs once; 1inch Aqua earns; ENS names it; the agent proposes and the Ledger decides.",
	"license": "MIT",
	"engines": {
		"node": ">=22 <23",
		"pnpm": ">=10"
	},
	"packageManager": "pnpm@12.3.4",
	"scripts": {
		"prepare": "[ -d .git ] && git config core.hooksPath .githooks || true",
		"dev": "pnpm --filter @moor/live-app dev",
		"dev:ledger": "pnpm --filter @moor/live-app dev:ledger",
		"agent": "pnpm --filter @moor/agent once",
		"agent:loop": "pnpm --filter @moor/agent loop",
		"check": "biome check .",
		"check:fix": "biome check --write .",
		"typecheck": "pnpm -r --if-present typecheck",
		"test": "pnpm --filter @moor/core test",
		"build": "pnpm --filter @moor/live-app build",
		"contracts:build": "forge build --root packages/contracts",
		"contracts:test": "forge test --root packages/contracts",
		"contracts:coverage": "forge coverage --root packages/contracts --report summary --ir-minimum",
		"contracts:fmt": "forge fmt --root packages/contracts --check",
		"contracts:deploy": "pnpm --filter @moor/contracts deploy:sepolia",
		"demo:taker": "pnpm --filter @moor/contracts demo:taker",
		"ledger:emu": "pnpm --filter @moor/erc7730 emu",
		"ledger:screens": "pnpm --filter @moor/erc7730 screens",
		"contracts:deploy:anvil": "pnpm --filter @moor/contracts deploy:anvil",
		"demo:ship": "pnpm --filter @moor/contracts demo:ship",
		"contracts:deploy:registrar": "pnpm --filter @moor/contracts deploy:registrar",
		"contracts:deploy:resolver": "pnpm --filter @moor/contracts deploy:resolver",
		"agent:bundle": "pnpm --filter @moor/agent bundle",
		"agent:serve": "pnpm agent:bundle && deno run --allow-net --allow-env --env-file=.env supabase/functions/agent-cycle/index.js",
		"agent:secrets": "node apps/agent/scripts/secrets.mjs",
		"agent:deploy": "pnpm agent:bundle && npx --yes supabase@2.116.0 functions deploy agent-cycle"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.12",
		"typescript": "5.9.3"
	}
}
